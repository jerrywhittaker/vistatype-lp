# Reported errors, and what fixed them

**Check this file before investigating any reported error.** It exists so that a fault already
diagnosed is never diagnosed twice — and so a report from someone running an older build can be
answered without reading a line of code.

Jerry's call, 8/26/2026, in his words: *"I don't want to spend time and tokens on
re-investigating errors that have already been fixed."*

## How a report arrives

From 3.0.261 a failed ribbon or toolbar button writes one line into
`%AppData%\VistaType LP Settings\VistaType-Errors.log` on the user's machine and shows them
dialog **240**, which asks them to send it. A line looks like this:

```
2026-08-26 13:42:47  v3.0.261  Lp_Video_Links  err 5941 "The requested member of the collection
does not exist."  step "Setting table alternating color style"  Word 16.0  doc "Document1"
cfg DEF  tmpl "Normal.dotm"  8.5x11 P  Tahoma 12
```

Everything the table below is keyed on comes off that one line: the **version**, the **error
number**, the **macro** and the **step**.

## How to use it

1. Take the version, error number, macro and step from the reported line.
2. Look for a row matching the error number **and** the macro.
3. **A match is strong evidence, not proof.** VBA gives no call stack, so the macro named is the
   one the *button* ran — the fault may live in something it called, and the same error number
   in the same macro can have two causes. On a match, confirm against that sub's own
   `' Version:` block before answering.
4. Compare their version against the two columns:

| | means | what to tell them |
|---|---|---|
| their version **≥ Shipped in** | they already have the fix | it is a different fault — investigate |
| their version **< Shipped in**, and Shipped in is filled | fixed and released | update to that release |
| **Fixed in** filled, **Shipped in** empty | fixed on `dev`, not released yet | known and fixed; it goes out with the next release |
| no row at all | not seen before | investigate, then add a row |

**Fixed in and Shipped in are different on purpose.** `3.0.X` numbers are private build
counters — nothing reaches a user until the next `X.Y` release. Telling someone to update to a
build that was never released sends them looking for something that does not exist.

## The standing rule

**A row is added in the same change as the fix, never afterwards.** A register that lags is
worse than none, because it says "not fixed" about something that is.

Record a fault here whether it arrived through the error log or Jerry found it himself — the
question this file answers is "has this already been dealt with", and where it came from does
not change that.

---

## The register

| Reported | Version | Error | Macro | Step | Cause | Fixed in | Shipped in |
|---|---|---|---|---|---|---|---|
| 8/26/2026, Jerry | 3.0.248 | none — wrong values, no error raised | `LP_Attach_An_Lp_Template_Form` (Attach LP Template dialog) | choosing a paper size, ticking Customize, ticking Mirrored | every media button cleared `CustomizeCheckBox` part-way down its own run; that assignment raises `CustomizeCheckBox_Click`, whose revert put `PTM/PBM/PLM/PRM` back from the previous media's `Hold_` values. Binding width and the margins the book was actually set with ran one media button behind | 3.0.249 | — |
| 8/26/2026, review | 3.0.249 | none — wrong page size, no error raised | `LP_Attach_An_Lp_Template_Form` (orientation handlers) | picking a tablet, ticking Customize, picking another tablet, picking the first again | both orientation handlers arranged the size boxes by testing `Hold_Orientation` (the orientation when Customize was last ticked) instead of the media's own. On a match they took the swap arm, and only paper had an `If IsPaper` arm below to put the numbers back — so a tablet reached Word with width and height transposed | 3.0.250 | — |
| 8/26/2026, review | 3.0.250 | any — swallowed, never seen | `Lp_Attach_The_Template` | anywhere after "Set Page Size and Orientation" | version 1.8 (10/24/2023) opened `On Error Resume Next` around the portrait-orientation line, for a real reason — it raises when the document starts with a drop cap — but never closed it. It stayed in force for the remaining 210 lines: the page size, the styles, the table banding, the TOC tab stops and a dozen `Application.Run` calls. Every failure in them was discarded and the user got a half-formatted book and no message | 3.0.251 | — |
| 8/26/2026, Jerry | 3.0.256 | 5941 and every other | all 47 ribbon and toolbar buttons | any failing macro | `RibbonAction` wrapped `Application.Run control.Tag` in `On Error GoTo`, but **Word does not pass an error back out of `Application.Run`** — it shows its own Run-time error dialog, which offers Debug, and the handler is never entered. Measured on the build box: four real button presses wrote a marker immediately before the call and never the one after it. Fixed by dispatching directly through the generated `Sh_Dispatch` | 3.0.257 | — |
| 8/26/2026, review | 3.0.262 | any — swallowed, never seen | `Lp_AutoTag_Page_Numbers` | anywhere after the roman-numeral loop | `On Error Resume Next` set inside that loop (`'prevents crash in a table`, right for the short block it guards) was never turned off, so it covered the remaining **339** lines: the `$pg` colouring pass, the paragraph mark taken off the top of the file, the screen-updating restore and the loop that counts the tags. A count that failed reported "There are no tagged page numbers in this document", which reads as a correct answer | 3.0.263 | — |
| 8/26/2026, review | 3.0.262 | any — swallowed, never seen | `Dx_AutoTag_Page_Numbers` | anywhere after the roman-numeral loop | the same fault on the braille side, covering the remaining **96** lines. Closed after the `LoopEnd:` label in both, which is the only placing that runs on every path — there are `GoTo LoopEnd` jumps earlier in each loop that would skip an `On Error GoTo 0` put at the end of the `If` block | 3.0.263 | — |
| 8/26/2026, Jerry | 3.0.264 | none — Word appears to hang, no error raised | `Dx_UnEmbed_Ref_Pg_No` | selecting something that is not a reference page number, or nothing to unembed | both early exits used `End`, which does **not** restore `Application.ScreenUpdating`. The screen was turned off eight lines above, so the user pressed Okay on the message and Word sat frozen. Now restores the screen and uses `Exit Sub` — provably equivalent, since the only caller is its own ribbon button — which also spares the module state `End` wipes (`gRibbon`, `Sh_ConfiguredAs`) | 3.0.265 | — |
| 8/26/2026, Jerry | 3.0.264 | none — Word appears to hang, no error raised | `LP_Picture_Alignment_Form` (`CmdOkay_Click`) | pressing Okay with nothing selected, or a picture that is not inline | the same fault: `End` after screen updating was turned off. The screen is restored before it now. `End` is deliberately KEPT here — this is a form's button handler, and `Exit Sub` would leave the dialog open and waiting, which is a decision about the dialog rather than a repair | 3.0.265 | — |
| 8/26/2026, Jerry | 3.0.265 | none — the list read wrongly, no error raised | `Sh_Copy_Ref_Pg_Tags_To_Temp_File` (Validate $pg Tags) | reading the validation list, braille side | the temp document was laid out in **2 columns** for braille and **3** for large print, so five separate paragraphs appeared on what looked like one line — only paragraph marks revealed it. That column width is also why a line was cut at 23 characters, and a braille entry (page number plus Duxbury code) runs to 37. Now one column, `SH_PGVAL_LINE_MAX` raised to 40. Applied to both sides: one constant, one builder | 3.0.266 | — |
| 8/26/2026, Jerry | 3.0.266 | none — Locate silently found nothing | `Sh_PgVal_TextOfCurrentTag` (Locate, in $pg validation) | pressing Locate on a braille entry | a tagged line is `$pg12-14[[*lec*]][[*i*]]14`, and the `[[…]]` Duxbury codes carry the **DBT Code** style, which is hidden. `Range.Text` returns hidden characters regardless of display, so Locate searched the book for a string that is not visible and matched nothing — as Word's own Navigation pane also fails to. Now takes the **leading visible run**, stopping at the first hidden character, which is contiguous in the book and is the tag plus page number. **Not cured:** the EBAE lower-roman shape `$pg[[*ii*]]v` puts the code ahead of the number, leaving only `$pg`, so Locate lands on the first tag. Curing that needs the list to remember where each entry came from rather than searching for its text | 3.0.267 | — |
| 8/26/2026, Jerry | 3.0.268 | none — reported as "not working", was **not a defect** | `Dx_Embed_Ref_Pg_No`, `Dx_UnEmbed_Ref_Pg_No` | pressing Embed or UnEmbed on a `$pg` page number | the numbers had not been through **Format $pg Tags**, so they carried none of the four reference-page styles and both macros correctly declined. Two things were investigated and DISPROVED along the way, recorded so nobody repeats them: `Selection.Style` **does** report a character style (measured on the build box — it takes it from the character to the LEFT of the insertion point, so inside the number or immediately to its right both work), and the detection code was never at fault. What changed is the message: a paragraph still showing a `$pg` tag is now told to run Format $pg Tags (241/243) instead of being told to select the number again (242/244) | 3.0.269 | — |
| 8/26/2026, Jerry | 3.0.270 | none — a dialog on every macro | `Dx_Is_BANA_Template_Attached` | any braille macro on a document prepared with Duxbury's SWIFT | SWIFT **does** attach the BANA template (`BANA Braille 2017.dot`, so the `InStr` test finds it) but does **not** set the `BrailleType` document variable — its only variable is `SWIFT.StyleMap.GUID`. The lookup raised, every braille macro jumped to Choose Translation, and nothing remembered the answer. The document had it all along: `DBTTemplate = "English (UEB) - BANA.dxt"` in its custom properties, which nothing read. Now derived and written once. **`PRE-UEB` is tested before `UEB`** — Duxbury names the EBAE tables `English (BANA Pre-UEB Textbook DE) - BANA.dxt`, which contains "UEB", so the other order would translate every EBAE book as UEB. SWIFT writes that property **on save, not on attach** (A/B tested), so a brand new unsaved document is still asked once | 3.0.272 | — |
| 8/26/2026, Jerry | 3.0.272 | none — refused a valid page number | `Dx_Manual_Tag_with_Dollar_pg` | typing `12` in its own paragraph and pressing Manual Tag Ref Page | it validated the line as a **roman numeral** and refused anything else, answering "This is not a valid roman numeral". Most page numbers are arabic, and the large print half of the same feature never validated at all. The roman test now only decides the EBAE `[[*ii*]]` code; anything holding a digit is tagged as it stands, and a line of prose gets dialog 245 | 3.0.273 | — |
| 8/26/2026, review | 3.0.272 | none — pages silently left untagged | `Sh_IsValidRomanNumeral`, used by **both** `Dx_AutoTag_Page_Numbers` and `Lp_AutoTag_Page_Numbers` | AutoTag Ref Pages on any book with roman front matter | a hand-typed list of 36 numerals that ran `I` to `X` then jumped to `XVI`. It accepted **28 of the first 100** — `xi`, `xii`, `xiii`, `xiv`, `xv`, `xxvi`–`xxix` were all rejected, which is exactly where front matter lives. Those pages were skipped in **braille and large print alike**, with nothing to say so, and the list is byte-identical in the repository's first commit. Replaced with a value round trip: read the numeral as a number, write it back, require a match. Now 100 of 100, with `IIII`, `VX`, `IC` still refused | 3.0.273 | — |
| 8/26/2026, Jerry | 3.0.272 | none — pages silently left untagged | both AutoTag macros (paragraph window) | roman page numbers of 388 or above | both loops skip any paragraph longer than **11** characters — 10 of text plus the paragraph mark — so `CCCLXXXVIII` (388) and every numeral above it was passed over even with a correct validator. 347 of the numbers below 4000 did not fit. Widened to **16**: `MMMDCCCLXXXVIII` (3888) is the longest below 4000 at 15 characters, so there is no gap from i to mmmcmxcix. Found because Jerry asked whether a wider range was possible | 3.0.273 | — |
| 8/26/2026, Jerry | 3.0.274 | Compile error in hidden module | `Sh_IsValidRomanNumeral` / `Dx_Manual_Tag_with_Dollar_pg` | any button, once installed | while widening the manual tag I dropped the `UCase()` from `Sh_IsValidRomanNumeral(UCase(Possible_LC_Roman))`, handing a **Variant to a ByRef `String`** — a compile error, and VBA compiles a procedure the moment it is reached, so it arrived as "Compile error in hidden module" rather than as a fault in that macro. The parameter is `ByVal` now, which was right anyway: the function reassigns its own parameter, so under ByRef it was mutating the caller's variable. **Every `tools/lib` guard passed it** — none type-check — and a build-time compile gate was attempted and proved impossible (see CLAUDE.md). The only full compile is **Alt+F11 → Debug → Compile**, by hand, now step 3a of the release checklist | 3.0.275 | — |
| 8/27/2026, Jerry | 3.0.277 | none — the spelling checker on in every braille file, no error raised | `MS_Set_Word_Config_For_Braille` | opening any braille document, and Full File Cleanup failing to clear it | not SWIFT and not the BANA template — **VistaType LP turns it on itself**. The braille configuration has forced `Options.CheckSpellingAsYouType = True` since 3.0.220 (8/21/2026), deliberately: the AutoCorrect "use suggestions from the spelling checker" box is gated on it and is silently refused while it is off. That is why the marks looked identical whichever add-in attached the template. Full File Cleanup could never clear it, but **not** because it misses the configuration — it reaches it twice over (`Dx_Is_BANA_Template_Attached` → `Dx_Choose_Translation_Form`'s `UserForm_Initialize`, and the menu's Fix Errors → `Dx_Fix_Common_File_Errors:3641`). Re-running the configuration is precisely what re-asserts the checker, so it was the source, not a gap. After this fix Full File Cleanup → Fix Errors **does** clear the marks. Cured by hiding the marks in the DOCUMENT instead: `ShowSpellingErrors` and `ShowGrammaticalErrors` False, Word's "Hide … errors in this document only". The app-wide setting, and so the AutoCorrect gate, is untouched, and ordinary documents are still checked. **Large print is deliberately not given this.** **Not cured:** `Options.CheckGrammarWithSpelling` is written nowhere in the project, so F7 in a braille file still checks grammar | 3.0.279 | — |
| 8/27/2026, Jerry | 3.0.278 | none — always the same wrong answer, no error raised | `Sh_Dbt_Translation_Table` (Doc Info, "DBT Translation table") | opening Doc Info on any braille document | it read one place — the custom property `DBTTemplate` — and **nothing in this project has ever written that property**: three reads, no writes. `DBTTemplate` is SWIFT's. A book the braille macros attached records the kind in the document *variable* `BrailleType` instead (`Dx_Choose_Translation_Form`, or `Dx_Set_BrailleType` on the derived path added the day before). So the one case the line was written for — a SWIFT file — was the only case it could ever answer. Now falls back to the variable and names the table from it via the new `Dx_Dbt_Table_From_BrailleType`, the exact inverse of `Dx_BrailleType_From_Dbt_Table`. The property is still asked first, since it names the actual `.dxt`. Where the document genuinely has nothing yet — SWIFT writes the property on **save**, not on attach — the line now reads "will appear here only after the file is saved" rather than "not recorded in this document", but only when saving would actually help (never saved, or changes pending). **Then done, same day, Jerry's call:** `Dx_Set_BrailleType` now writes `DBTTemplate` as well, so a book prepared here is legible to SWIFT too, and the Choose Translation dialog's four buttons route through it instead of writing the variable themselves. It **never overwrites an existing** `DBTTemplate` — DBT ships 226 tables and this project knows four, so rewriting one would risk pointing the book at a different table than was chosen in SWIFT. **Not verified:** no SWIFT install exists on the build box, so what SWIFT does with the property is inferred | 3.0.280 | — |
| 8/27/2026, Jerry | 3.0.281 | none — one screen disagreeing with itself, no error raised | `Sh_Doc_Info` (braille branch) | opening Doc Info on a SWIFT book before any braille macro has been run on it | it named the translation table on one line and said "Document UEB or EBAE translation settings are undefined" four lines below. The question *which translation is this* was written out longhand in `Dx_Is_BANA_Template_Attached` and nowhere else, so Doc Info answered it its own way — reading the `BrailleType` variable directly, which SWIFT never writes. Now one function, `Dx_Ensure_BrailleType`: the variable when it holds one of the four, else derived from SWIFT's `DBTTemplate` and recorded, else "". Doc Info calls it with `recordIt` False, because a screen that only reports must not mark the file as changed. Legacy `UEB`/`EBAE` still count as not knowing — they say neither textbook nor Nemeth. **Same day, same screen:** the sentence at the bottom naming the translation the macros will use still read "Document UEB or EBAE translation settings are undefined" on an unsaved SWIFT book, which reads as something broken when the real answer is that SWIFT has not written it yet. It now reads "The translation used by the macros will appear here only after the file is saved". Both that line and the translation-table line turn on one function, `Sh_Doc_Not_Yet_Saved`, so the two cannot disagree about one file | 3.0.283 | — |
| 8/27/2026, Jerry | 3.0.284 | none — two questions silently stopped being asked | `Dx_Attach_BANA_Template` | attaching a BANA template, after any braille document has been opened or switched to earlier in the same Word session | "Do you want to fix common file errors?" and "remove multiple consecutive paragraph marks?" were gated on the shared scratch string `Dx_GP_String_1` holding `"Doc_Is_Already_Brl"`. That value was written in **one** place — `Sh_Apply_Word_Config`'s braille branch, which runs every time a braille document is opened or switched to — and cleared in **one** place: inside the very `If` that tested it, on the branch where it did *not* hold that value. So once set it was never cleared, and the questions were skipped on every document for the rest of the session, including a fresh source file that had never been near braille. Whether the book was already braille is now read off the **document** before the attach, which cannot go stale and survives the `End` statement that wipes module variables. The write in `Sh_Apply_Word_Config` is removed — it was the only writer and the attach the only reader | 3.0.285 | — |
| 8/27/2026, Jerry | 3.0.285 | none — a message stopped appearing, no error raised | `Dx_Replace_Multiple_Para_Marks_With_Warning` | Remove Multiple Consecutive Paragraph Marks, from the braille cleanup menu | "Multiple paragraph marks deleted" had been gone since **8/12/2026** (`361ea09`). It lived in the braille side's own worker, `Dx_Replace_Multiple_Para_Marks_No_Warning`, which was deleted whole when both sides were merged onto the shared `Sh_Replace_Multiple_Para_Marks_No_Warning` — correctly, since it worked through a temporary document and could not see a blank line holding a space. The closing message went with it and was never carried across. Large print never had one (it shows a please-wait box instead), so nothing flagged the gap. Restored on the braille wrapper only — the shared worker has five other callers that should stay quiet. `Sh_Say`, dialog **246**. **Put back in the wrong place first:** it went only on the cleanup menu's button, leaving the attach's own "remove multiple consecutive paragraph marks?" question silent — which is the route Jerry was on, having just had those questions restored. Both routes say it now, same message and same number. The Selected Cleanup form is deliberately still silent: it runs several ticked jobs and one dialog each would be a queue of them | 3.0.287 | — |
| 8/27/2026, Jerry | 3.0.288 | none — improvement, not a fault | `Sh_Replace_Multiple_Para_Marks_No_Warning` | the message after removing empty paragraph marks, braille side | it said "Multiple paragraph marks deleted" whether it had removed two hundred or none, so it confirmed the macro had *run*, not that it had *done* anything. It now counts what it deletes and returns the number, and both braille routes report it — including "No empty paragraph marks were found", which tells the transcriber the file was already clean instead of leaving her guessing. Sub → Function, safe because the name is not a ribbon, toolbar or keyboard target; the four callers that do not want the number still reach it through `Application.Run` as a statement, unchanged. `deCount` was **not** the answer — it counts paragraphs walked, to throttle DoEvents. One `Dx_Para_Marks_Message` builds the sentence for both routes | 3.0.289 | — |
| 8/27/2026, Jerry | 3.0.290 | none — a gap in a list, not a fault | `Sh_Add_Compact_Fractions` and `MS_Set_Word_Config_For_Large_Print`'s delete list | typing a compact fraction, and removing them from a large print book | both lists carried **18** of Unicode's **19** precomposed vulgar fractions. Enumerated character by character rather than recalled: U+00BC–U+00BE and the whole run U+2150–U+215E were all present; the one missing everywhere was **U+2189 ↉ VULGAR FRACTION ZERO THIRDS (0/3)**. There is no twentieth — U+2044 FRACTION SLASH and U+215F FRACTION NUMERATOR ONE carry "FRACTION" in their names but are parts, not values, and every other is script-specific (Tamil, Malayalam, Oriya, Meroitic…). Added to **both** lists, which are verified to name the same 19. Coverage measured first: Times New Roman (braille books), Tahoma and VistaTypeLP Sans all have it; Calibri and Arial do not, but they already cannot draw 1/7, 1/9 or 1/10 — Arial misses nine of the eighteen — so this joins an existing exposure rather than making one | 3.0.291 | — |
| 8/29/2026, Jerry | 3.0.292 | none — a cleanup step that had never once run | `Dx_Fix_Common_File_Errors` → `Dx_Replace_Fraction_Text_With_Compact_Fractions` | braille Full File Cleanup, typed fractions in the source file | Jerry asked for fraction conversion to be added to braille Full File Cleanup and found the macro already existed — written 10/1/2021 and called from **nowhere**: no ribbon button, no menu, and not from this sequence, though its own 10/20/2021 note claims it was added to it. Word's own AutoFormat command had been doing fractions instead, until that was removed on 8/18/2026 for eating empty paragraphs, so braille books had no fraction pass at all for eleven days. **It could not simply be switched on.** All eighteen of its passes set `MatchWholeWord = True`, which reads as though it stops `1/2` being found inside a date. Measured on the build box: Word ignores whole-word entirely once the search text carries a slash — `Due 1/2/2026 and 3/4/2025` came back `Due ½/2026 and ¾/2025`, and `21/2` came back `2½`. Rewritten as one guarded helper per fraction (`Dx_Compact_One_Fraction`) that reads the character on each side of a hit and refuses one with a digit or a slash against it; `2 1/2 cups` still converts. It finds every hit first and rewrites them **last one first**, because with track changes on a replacement is retained as a marked deletion and a re-scanning loop would find its own output for ever. Two more faults went with it: `5/8` was searched for as `" 5/8"` with a leading space, so it ate the space in front (`Take⅝ inch`), and the two trailing passes that hid every `[[*code*]]` in plum — leftovers from the 3/5/2023 edit that stopped this macro writing DBT fraction codes — are gone, since `Dx_Set_DBT_Codes_Color_and_Style` does that properly at the end of the sequence *and* applies the DBT Code style. `0/3` is deliberately **not** swept: in real text it is a score or a count | 3.0.293 | — |
| 8/29/2026, review | 3.0.292 | none — a compact fraction left standing in a large print book | `Lp_Replace_Compact_Fractions_With_Fraction_Text` | large print Full File Cleanup, and attaching the LP template | it took apart **18** of the 19 and had no case for `ChrW(8585)` — `↉`. Harmless until 8/27/2026, when `0/3` was added to the AutoCorrect list: from that day a transcriber typing `0/3` into a large print book got a character that nothing here ever expanded again, drawn far smaller than the size the book is set in. That is large print's cardinal rule broken, silently. One block added, matching the other eighteen. Found while adding the braille sweep above, not by a report | 3.0.294 | — |
| 8/29/2026, Jerry | 3.0.292 | none — a date turned into a fraction, no error raised | `MS_Set_Word_Config_For_New_Install` and `MS_Set_Word_Config_For_Braille`, through the shared list sub | typing `1/2/2026` in an ordinary document, or in a braille one | an AutoCorrect entry named `1/2` fires the moment `1/2` is followed by **any** delimiter, and a slash is one — so the second slash of a date triggers it and `1/2/2026` becomes `½/2026`. This is what an AutoCorrect entry does; no entry can be written that tells a date from a fraction. The entries went into the ordinary-document configuration on 8/18/2026 (Jerry's call then: an ordinary document should have all eighteen) and had been in the braille one since 10/19/2021. **Both now delete them instead** — `Sh_Add_Compact_Fractions` was *renamed* to `Sh_Delete_Compact_Fractions` rather than a second sub added beside it, so nothing in the project can put them back. Deleting matters as much as not adding: an entry, once written, stays in the transcriber's own AutoCorrect list, and every machine that ran a 3.0.2xx build has all nineteen. Nothing is really lost — Word's own fraction replacement does ½ ¼ ¾ through `AutoFormatAsYouTypeReplaceFractions`, a **different** mechanism that has been on the whole time and has never touched a date, and braille gets the rest from Full File Cleanup, which reads both sides of a hit. An ordinary document keeps the other fifteen as typed, exactly as before 8/18/2026. **Large print is untouched** — it has deleted all nineteen since 10/19/2021 and keeps its own copy of the list, because the blanket `On Error Resume Next` above that block also covers the rest of that sub. Verified on the build box: 19 of 19 seeded, 0 of 19 after one run, and a second run on a machine with none raised nothing | 3.0.295 | — |
| 8/29/2026, Jerry | 3.0.296 | none — the DBT codes came out as ordinary black text | `Dx_Type_Dashes_Form` → `CmdEncode_Click`, now `Dx_Encode_Fractions_With_DBT_Codes` | pressing **Encode Fractions** on the Dashes/Primes/Fractions dialog | it **did** hide them — three passes, one each for `[[*fs*]]`, `[[*fl*]]` and `[[*fe*]]` — and then its last two statements undid all of it. `Selection.Font` applies to the **whole selection**, and the reset to not-hidden and automatic ran **before** `Selection.Collapse`, so the very last thing the macro did was un-hide and un-plum every code it had just spent three passes hiding. The reset is still wanted — without it the next thing the transcriber types comes out hidden and plum — it simply belongs on the insertion point. Exactly the fault the **Enter Fraction** button had, cured the same way on 2/24/2023; this one was never noticed because a code that looks like text still translates. Measured on the build box before and after | 3.0.300 | — |
| 8/29/2026, Jerry | 3.0.300 | none — a date turned into a coded fraction | `Dx_Encode_Fractions_With_DBT_Codes` | pressing **Encode Fractions** over a range holding a date | one wildcard `Replace:=wdReplaceAll` did the whole job, so on `12/25/2026` it matched the `12/25`, stopped, and produced `[[*fs*]]12[[*fl*]]25[[*fe*]]/2026` — a date turned into a fraction with its year hanging off the end. It now reads the character on each side of every hit and refuses one with a **slash** against it, which is the whole of what separates `nn/nn/nn` from a fraction. Note this is a **different** rule from `Dx_Compact_One_Fraction`'s, which also refuses a digit: that one searches for the literal `1/2` and would otherwise match inside `21/2`, while this pattern takes every digit it can reach, so `21/2` arrives as the improper fraction it is and is encoded. **Not cured, and cannot be:** a two-part date like `12/26` is character for character a fraction. The words above the button say so. The logic also moved out of the form's button handler into `LPandBrlMacros`, where the rest of this project keeps its macros. Measured: `12/25/2026` and `1/2/2026` untouched, `2 1/2`, `21/2` and `1.5/2` all encoded | 3.0.301 | — |
| 8/29/2026, review | 3.0.297 | any — swallowed, and the progress bar left frozen on screen | `Dx_Attach_BANA_Template` | anywhere after the view settings, which is most of the macro | version 2.3 (1/9/2023) opened `On Error Resume Next` for the one line that needs it — `CommandBars("Navigation")`, which does not exist in every Word build — and never closed it. It stayed in force for the rest of the sub: the translation choice, the foreign language coloring, the text box removal. Harmless while everything after it went through `Application.Run`, which never propagates anyway. **Not harmless from 3.0.297**, when the attach began running the two cleanups directly and behind a modeless progress bar: a raise in either one would resume at the next statement with the bar never closed and `ScreenUpdating` never restored, `RibbonAction` would never see it, and nothing would be logged. The transcriber gets a box stuck at 47% over a Word that will not repaint — the "it hung" report. The block moved to the end of the macro (Jerry's four-stage order) and the trap is now closed. The **fourth** row of this kind; see 3.0.250, 3.0.263 twice | 3.0.298 | — |
| 9/1/2026, Jerry | every build up to 3.0.325 | none — **the book comes out corrupted, and Word can close outright**; nothing raised, nothing logged | `Lp_Section_Brk_Caution` (`CmdOkay_Click`), reached from Selection Cleanup → replace section breaks | running it on a book whose chapters start on odd pages | **A DEFECT IN THE FEATURE, NOT IN THAT DAY'S WORK — it had been there all along.** The macro replaced every section break with a manual page break. That collapses the whole book into **ONE section**, so every section's page setup is discarded and the book takes the first section's. On Jerry's own *Book with Front Matter - 5 chapters Page Per Chapter.docx* — eight sections, seven of them `wdSectionOddPage`, each sitting immediately before a `Print Pg Num` paragraph — the result was "fine before the macro, corrupted after", and asking Word to repaginate what came out killed Word (`0x800706BE`). Measured both ways: the old `Selection.Find`/`wdFindContinue` form and the `Range.Find` form both collapse 8 sections to 1, and both leave the book unable to repaginate. **Two things I got wrong on the way, recorded so they are not repeated:** no paragraph mark is destroyed — the paragraph count is 62 before and after and every `Print Pg Num` paragraph survives at the same index; and `Range.Find` is not the culprit, the section collapse is. **Cured by not deleting the breaks at all.** Whether a section begins on the next page or the next *odd* page is a **property** of that section, so the macro now sets `SectionStart` from `wdSectionOddPage` / `wdSectionEvenPage` to `wdSectionNewPage`. Every section and its page setup survive, no paragraph mark is touched, the `Print Pg Num` paragraphs are not involved, the blank filler pages a double-sided book needs are gone and each chapter still opens on a page of its own — which is the whole purpose of the button (Jerry: "change the book from a double-sided document into one which can be used on the continuous format of a tablet screen while preserving the start of each chapter on a new screen page"). It works on the **whole book** now, never a selection, and no longer asks for one. Both dialogs were reworded to match, and dialog **129** ("End of Macro") is retired. | 3.0.326 | — |
| 8/29/2026, review | 3.0.297 | none — a silent, un-undoable cleanup on fifteen buttons that never asked for one | `Dx_Is_BANA_Template_Attached` → `Dx_Attach_BANA_Template` | pressing Manual Tag, AutoTag Ref Pages, Embed, UnEmbed, Type Dashes, Compress Linear Math or nine others on a document with no BANA template | those fifteen macros attach the template **for** the transcriber when the document has none. With the attach's cleanup made automatic in 3.0.297, all fifteen would have begun running thirty-six destructive passes — every image, text box, empty paragraph and keep-with-next gone — followed by `ActiveDocument.UndoClear`, because she asked to tag **one page number**. Until then those routes reached two Yes/No questions she could decline. Caught in review before it shipped. `Dx_Attach_BANA_Template` is now a wrapper passing `autoClean` True and `Dx_Is_BANA_Template_Attached` calls the worker with False, so the automatic cleanup belongs to the Attach button alone (Jerry's call). Large print reaches the same shape by a different route: its one implicit attach only fires on a document that is already LP, so its cleanups were already skipped — and its `"Bypass Cleanup Checks"` flag, written in `Sh_HandleDocumentOpened`, is read **nowhere** and never was | 3.0.298 | — |
| 9/2/2026, Jerry | 3.0.332-3.0.336 (never released) | none - **Word closes outright**, access violation `0xc0000005` in `wwlib.dll`; nothing raised, nothing logged | `Lp_TOC_CleanAndFormat_TOC`, reached from Table and TOC Tools -> Format the TOC | the first find-and-replace pass, immediately after the custom undo record is opened | **A CUSTOM UNDO RECORD, ADDED BY THE CONVERSION - NOT A FAULT IN THE FEATURE.** Taking the macro off the scratch document (3.0.330) also wrapped the job in `Application.UndoRecord.StartCustomRecord "Format TOC"`, so that Ctrl+Z would take it back in one press instead of two. A `Find` with `Replace:=wdReplaceAll` inside a custom undo record kills Word. **Measured on the build box with three canaries**, each doing the same harmless range replace under one condition: screen on, no undo record - SURVIVED; screen frozen, no undo record - SURVIVED; screen frozen, undo record open - never came back. Cured by removing the undo record. **Four things were cleared first and must not be re-investigated:** the document and the selection (paragraphs 14-27, 826..1011 of *01 - TOC Tutorial - Formatting and Correcting Errors.docx*) - the whole sequence completes when Word is driven step by step from a script against a copy; the Find object being reused across passes - each pass now takes a fresh `Duplicate` and it still died; the inherited find state - recorded clean, no wildcards, no word forms, no sounds-like; and the macro logic itself. **It could not be reproduced headlessly at all** - the macro runs while `Lp_TOC_Format_And_Color_Form` is hidden but still loaded and modal, and a script cannot drive a modal dialog, so the answer came from tracing markers written to a file by the build in Jerry's own Word. Ctrl+Z now takes several presses and dialog 207 no longer promises a number | 3.0.338 | - |
| 8/30/2026, Jerry | 3.0.303 | 5825 "Object has been deleted" | `Lp_Import_Exported_Selection_File`, `Dx_Import_Exported_Selection_File` | choosing the document itself in Import's file dialog | `Documents.Open` on a file that is **already open** hands back that same document, so the source and the destination became one. The macro inserted the text, then closed "the source" — deleting the document every remaining range still pointed into, which is why the error arrived *after* the text had gone in rather than instead of it. Both macros now compare the chosen path against `ActiveDocument.FullName` before opening anything, and say a file cannot import itself (**247** large print, **248** braille) | 3.0.304 | — |
| 8/30/2026, review | 3.0.304 | none — the transcriber's clipboard silently replaced, and a windowless document left open | `Dx_Import_Exported_Selection_File` | any braille import, and any failure part way through one | it moved the text with `sourceRange.Copy` + `Selection.PasteAndFormat`, so **every** import destroyed whatever the transcriber had on her clipboard, and it was exposed to Error 4605 and the 0x5 clipboard crash that the large print half had already been rewritten to avoid. Its error handler also never closed the source document, which is opened `Visible:=False` — a failure between opening it and reading it left a document open in the session with **no window**, which she can neither see nor close. Given the large print body: direct range transfer, insertion point fixed before the dialog opens, source closed in the handler. `Lp_Normalize_Styles` deliberately **not** carried over — it is large print's and has no braille equivalent | 3.0.305 | — |
| 8/30/2026, review | 3.0.306 | any — swallowed, and background saving left switched off for Word itself | `Lp_Export_Selection_To_NewFile` | any failure during Export Selection To New File | it had **no** `On Error` handler at all — the `CleanExit` label was reached only by running off the end of the sub. So from the moment it set `Application.Options.BackgroundSave = False`, any raise left that switched off for **Word**, application-wide and surviving a restart, with `ScreenUpdating` off and the hidden export document still open, and reached the transcriber as Word's own Run-time error dialog offering **Debug**. Now handled: reported through `Sh_Say` (**253**) naming the macro and the error number, and every path leaves through the cleanup. The **fifth** row of the swallowed-or-never-restored kind; see 3.0.250, 3.0.263 twice, 3.0.298 | 3.0.307 | — |
| 8/30/2026, review | 3.0.306 | none — background saving switched off by pressing Cancel | `Dx_ExportSelectionToNewFile`, and the same shape in `Lp_Export_Selection_To_NewFile` | pressing Cancel on the Export Selection file dialog | cancelling ran `GoTo CleanExit`, which restores `Application.Options.BackgroundSave` from `originalBackgroundSave` — but the capture happens two steps **later**, so it wrote an uninitialized `False` into Word's own setting. Cancelling an export therefore caused the exact fault the restore exists to prevent, on the most ordinary path there is. Both macros now set a `bgSaveCaptured` flag where the value is read and restore only when it is set | 3.0.307 | — |
| 8/30/2026, review | 3.0.306 | 13 "Type mismatch" | `Dx_Is_BANA_Template_Attached` | any braille macro on a document carrying a BANA template older than 2014 | the warning passed its **title** where `MsgBox` expects the **buttons** argument — `MsgBox("…", "Braille Macros")` — and a string cannot convert to `VbMsgBoxStyle`, so the dialog raised instead of appearing. It is now `vbExclamation, "Braille Macros (263)"`. Only reachable on a pre-2014 template, which is why it survived; found while numbering every braille dialog, not by a report | 3.0.307 | — |
| 8/30/2026, review | 3.0.306 | none — one character of the transcriber's text deleted, no error raised | `Lp_Export_Selection_To_NewFile` (the "SMART DELETE" block) | exporting **part of a paragraph** and answering Yes to "Delete the selected text from this file?" | when the selection did not end at a paragraph mark it extended by one character and deleted that too, **without looking at what that character was** — so a mid-paragraph export took the following character of her text with it. A whole-paragraph selection ends in `Chr(13)` and was unaffected, which is why it survived. It now reaches past the selection only when that character **is** a paragraph mark, and the whole delete is wrapped in an undo record so one Ctrl+Z restores it, which dialog **252** says | 3.0.307 | — |
| 8/30/2026, review | 3.0.306 | none — two bookmarks left in the transcriber's document | `Dx_ExportSelectionToNewFile` | pressing Export Selection on a document that has never been saved | `DxExportStart` and `DxExportEnd` were added **before** the "file must be saved" test, and that test leaves by `Exit Sub`, which does not pass through `CleanExit` — so every time it fired it left both bookmarks behind in her file. The test now runs before they are added. **Knowingly not cured** (Jerry's call, 8/30/2026): on the normal path the bookmarks do reach the disk, because the saved copy is what gets cloned, and they are removed from the document afterwards but it is not saved again — so they sit in the file until her next save. They are invisible, they do not affect DBT, and a second save is the slow part of the macro on OneDrive | 3.0.307 | — |
| 8/30/2026, review | 3.0.306 | none — a .docm holding .docx contents | `Dx_ExportSelectionToNewFile` | exporting a selection out of a macro-enabled document | it saved with `FileFormat:=srcDoc.SaveFormat`, so a `.docm` source produced a macro-enabled export — and neither export macro forced the **file name** to agree with the format it wrote, so naming the file `.docm` gave a file whose contents and name disagree and which Word objects to on opening. Both now write `wdFormatXMLDocument` and force the name to `.docx` through `Lp_Force_Docx_Extension` / `Dx_Force_Docx_Extension` (Jerry's call, 8/30/2026) | 3.0.307 | — |
| 8/30/2026, Jerry | 3.0.306 | none — a blank line at the end of every exported braille file | `Dx_ExportSelectionToNewFile` | any braille Export Selection | one line guaranteed the file ended in a paragraph mark and the **next added a second unconditionally**, so every export carried a trailing blank paragraph — a blank line in the braille. The comment beside it read "but keeping your logic", so it was left standing and put to Jerry rather than removed on a guess; his call, 8/30/2026, was to remove it | 3.0.307 | — |
| 8/30/2026, review | 3.0.306 | none — improvement, not a fault | every braille macro that shows a message | reading a dialog number off a braille dialog | **29 braille dialogs carried no number at all** — all of them said only "Braille Macros", so a transcriber reporting what she saw could not identify which one it was. They are numbered **248–282**, keeping "Braille Macros" as the wording so she still sees which tab she is on. Worth knowing when reading an older report: **before 3.0.307 no braille dialog had a number.** Also corrected here — numbers 241–246 had been assigned twice, because the search that picked "the next unused number" looked only for `"VistaType LP (nnn)"` and so never saw the braille dialogs already using `"Braille Macros (nnn)"`. Every number in the project is now unique, 123 to 282; **240 appears twice on purpose**, being one dialog reached two ways | 3.0.307 | — |
| 8/31/2026, Jerry | 3.0.308 | none — the screen goes blank while the macro runs | `Dx_Format_Exercise_Lv_1_and_Lv_2` | any braille Exercise Levels 1 & 2 | what she is looking at during those seconds is the SCRATCH DOCUMENT. `Dx_Copy_To_Temp_Doc` made it with a plain `Documents.Add`, which creates a document that is visible and active, and it had to: every pass in the macro worked through `Selection`, and `Selection` only reaches the document that is active. The passes work on a RANGE now and the scratch document is created `Visible:=False`, so nothing is ever shown or activated. **This button was ticked as converted on 8/13/2026 and was not** — what was tested that day was Convert Auto List To Text, a helper it calls; see `docs/Temp-Doc-Conversion-Checklist.md`. Its large print twin, `Lp_Format_Exercise_Lv_1_and_Lv_2`, carries the same wrong tick and still round-trips | 3.0.309 | — |
| 8/31/2026, review | 3.0.308 | none — the transcriber's clipboard silently replaced | `Dx_Format_Exercise_Lv_1_and_Lv_2` | any braille Exercise Levels 1 & 2 | `Dx_Copy_To_Temp_Doc` moved the text with `Selection.Copy` and `Dx_Copy_From_Temp_Doc` brought it back with `Selection.Paste`, so every run threw away whatever she had copied. The text moves by `FormattedText` now - the same repair the braille import had on 8/30/2026. `Sh_SleepForSeconds 3` went with it: three seconds of every run spent waiting for a clipboard that is no longer involved. **Only half cured, and deliberately so:** the macro still ends with `MS_Clear_F_and_R_Params_and_Clipboard`, which empties the clipboard on purpose and always has, so her copy is still gone when the macro finishes. Whether that line should stay is Jerry's call, not a fault. What DID change beside it: `Dx_Fix_Para_Space_Errors` was clearing the clipboard even when run as one step of a longer job, which `Sh_Remove_Multi_Spaces` has never done - that call now sits inside its `If onOwn` like the other one's | 3.0.309 | — |
| 8/31/2026, review | 3.0.308 | none — every tab in the list replaced with NOTHING | `Dx_Tabs_To_Fill_Ins` | Exercise Levels 1 & 2 on a document whose `BrailleType` is neither UEB nor EBAE | the pass that turns each tab into the fill-in indicator assigned `.Replacement.Text` only inside two `If`s - one for EBAE, one for UEB - and ran regardless. With neither matching it kept whatever the PREVIOUS pass had left on the shared `Selection.Find` object, which was a period, so every tab in the list became one. The same shape sat in the level 2 fill-in pass in the macro itself, where it would have replaced every level 2 paragraph with nothing. Both now work out the indicator first and do nothing at all when there is none. Never reported: `Dx_Ensure_BrailleType` settles the type before these macros run, so reaching them with a third value takes a document that has been round the houses | 3.0.309 | — |
| 8/31/2026, review | 3.0.308 | none — a fault that could not fire, removed with the code around it | `Dx_Format_Exercise_Lv_1_and_Lv_2` (the two fake "99. " entries) | any braille Exercise Levels 1 & 2 | the macro puts a fake numbered entry at each end of the list so the find that places `[[*kps*]]` and `[[*kpe*]]` - which looks for a paragraph mark followed by a number - has something to find at each end, then takes them away again. Both were typed with the cursor, and the one at the BOTTOM relied on the selection's trailing paragraph mark having left an empty paragraph to land in - which the "remove multiple para marks" pass immediately above it had just collapsed. The removal afterwards deleted by display line and by counted characters, so had the fake ever landed at the end of a real entry's line it would have taken that entry with it. Each is now removed only after its text is checked, so a real entry can no longer be taken by mistake | 3.0.309 | — |
| 8/31/2026, review | 3.0.308 | none — an exercise formatted with the wrong styles and no fill-in indicators, silently | `Dx_Format_Exercise_Lv_1_and_Lv_2` | Exercise Levels 1 & 2 on a document whose `BrailleType` is not one of the four current values — after pressing **Cancel** on the translation-choice dialog, or on a pre-2024 file still carrying the legacy `"UEB"` / `"EBAE"` | every pass in the macro asks whether the type is `UEBT`, `UEBN`, `EBAT` or `EBAN`. Anything else fell through all of them: the exercise was formatted with `Exercise2` instead of `Ex2Nemeth2` and no fill-in indicator was written at all, and nothing said so. It now stops and says which is missing (**284**). Two halves to the old fault - the read itself, `ActiveDocument.Variables("BrailleType")`, RAISES on a document that has no such variable (a Cancel leaves none), and it goes through `Dx_Ensure_BrailleType` now, which answers `""`. **The legacy values are not mapped onto the new ones** - which of the four a `"UEB"` document wants is a braille question, not a code one | 3.0.309 | — |
| 8/31/2026, Jerry | 3.0.313 | none — the last answer left unclosed, and a line of its own reading `[[*kpe*]]` | `Dx_Format_Exercise_Lv_1_and_Lv_2` (the bottom marker entry) | any braille Exercise Levels 1 & 2 | **caused by 3.0.309, not by the old code.** The macro puts a fake `99.` entry after the last answer so the find that places the markers — a paragraph mark followed by a number — has something to find there. The old cursor code typed it INTO the empty paragraph that the selection's own trailing mark leaves at the end of the scratch document, and depended on that paragraph being there. 3.0.309 read that dependency as fragile and added a paragraph mark of its own instead: that put an EMPTY paragraph between the last answer and the fake, so the `[[*kpe*]]` that closes the last answer attached to the empty paragraph. Jerry: "the [[*kpe*]] should follow the last answer and should be `d. Temper[[*kpe*]]`". The dependency was not fragile, it was load-bearing. It now uses the empty paragraph when there is one and adds a mark only when there is not | 3.0.315 | — |
| 8/31/2026, Jerry | 3.0.310 | 5904 "Cannot edit Range." | `Dx_Format_Exercise_Lv_1_and_Lv_2` → `Dx_Ex_Delete_Para` | any braille Exercise Levels 1 & 2 — the macro stopped and reported, having done nothing | **also caused by 3.0.309.** Removing the bottom marker entry asked Word to delete the last paragraph's TEXT while stopping short of the document's permanent final paragraph mark, and Word refused. That trimmed shape was added in 3.0.309 on a review's worry that a plain delete of the last paragraph might be refused; the opposite is true — the plain delete is what Word allows, and it keeps the permanent mark by itself. Found by the step marker in the log (`Sh_Last_Activity`), which named "removing the marker entry at the bottom [paragraph 70 of 70, 1945-1959, document ends 1959]" and cost one run instead of a search. Ruled out first, on the build box, against Jerry's own file: the windowless scratch document, the hidden text in that paragraph, and every other range operation the macro performs. `Dx_Ex_Delete_Para` now tries the plain delete, then the trimmed one, then the shape the old cursor code used, and reports with the paragraph's numbers if all three are refused | 3.0.313 | — |
| 8/31/2026, review | 3.0.311 | none — a book's text round-tripped through a Word 2003 document | `Dx_Exercise_Levels_Hidden` and `Lp_Horz_To_Vert_Hidden` | Exercise Levels 1 & 2, and Horiz List to Vertical on a BRAILLE document | both build the scratch document with `Documents.Add(Template:=...)` from the source's own attached template, and a document made from a `.dot` inherits that template's compatibility mode. Measured on the build box: BANA Braille 2017 is a `.dot`, and the scratch document came out in **mode 11 — Word 2003** — while the transcriber's book was mode 15. The old braille route never had this, because `Dx_Copy_To_Temp_Doc` made a plain document and attached the template afterwards. Both now put the scratch document back up to the book's own mode, trapped so it can never be fatal. Not the cause of the 5904 above — that was ruled out separately | 3.0.312 | — |
| 8/31/2026, Jerry | 3.0.318 | none — the font name, font size, language and style boxes all go blank | not a macro — Word's own behavior | selecting the WHOLE of a formatted exercise; one word, or a sub-range, is fine | **not a fault, and worth the row so it is not investigated again.** Word shows a value in those boxes only when the selection holds ONE value, and empties the box when it holds more. Measured off Jerry's own `Exercise Levels 1 & 2.docx`: the `DBT Code` character style is **DejaVu Sans 12** and is what the macro puts the `[[*kps*]]` / `[[*kpe*]]` markers into — it must, because that style is how Duxbury recognizes a DBT code — while the text around them is **Times New Roman 14**, the document's Normal. The `French` character style is DejaVu Sans 12 in French, on two of question 16's answers. And the exercise ends in **two** paragraph styles, Exercise1 and Exercise2. So a full-exercise selection spans two fonts, two sizes, two languages and two paragraph styles, and all four boxes empty together. Nothing to do with the 3.0.309 conversion; a formatted exercise has always been mixed. Confirmed by Jerry the same day on a fresh document | not a fault — no change | — |
| 8/31/2026, Jerry | 3.0.318 | none — Full File Cleanup and Remove Empty Paragraphs do not run on attach | `Dx_Attach_BANA_Template_Run` | pressing **Attach BANA Template** on a file that ALREADY carried a BANA template | **not a fault — this is the guard doing its job.** `wasAlreadyBrl` is read off the document before the attach, and the two cleanups run only when the file was not already a braille book. Jerry's own condition, 8/29/2026: re-attaching to a book already in braille is a repair, and thirty-six passes over corrections already made would undo them without asking. Worth knowing about the second way in: the fifteen macros that attach the template quietly through `Dx_Is_BANA_Template_Attached` pass `autoClean` False, so a file tagged before it was attached counts as "already braille" afterwards and gets no cleanup from either road. Confirmed by Jerry the same day — a fresh document, attach, format, all correct | not a fault — no change | — |

| 9/3/2026, Jerry | 3.0.339 | none — a splash of the table's yellow rows across the screen, and a full-screen blink | `Lp_Table_Convert_Options_Form` (Convert Table to List, Rotate Table) | any table conversion on the Table Tools menu | Jerry: *"the table rotation shows flashing yellow on the screen during the operation... full screen blinking"*. Both conversions ran inside a scratch document that `Lp_Copy_To_Temp_Doc` creates hidden and then deliberately **shows, maximizes and activates** — four statements, because every pass reached its table through `Selection` and `Selection` only reaches the active document. That window holds one table and nothing else, which is the yellow. The rotation then finished by **minimizing and maximizing the Word window on purpose** — its own comment reads "the Jolt - Minimize/Maximize forces Windows OS to repaint the pixels" — which is the blink. Both conversions now work on the table where it stands in the transcriber's own book: ten macros take the table as an argument, `Lp_Table_Transpose_Table` hands back the table it built, and the clipboard round trip, the two document activations and the temp file are gone. **The round trip itself came back at 3.0.348, hidden** — see the row below for why removing it was the wrong half | 3.0.348 | — |
| 9/3/2026, Jerry | 3.0.339 | none — every column left selected when the macro finishes | `Lp_Convert_Table_To_Real_Columns` | Table Tools → Real Columns, after pressing Okay on the message | Jerry: *"real column ends with all columns selected"*. Step 8 selects the converted text to set its paragraph spacing and step 9 needs that selection to reach the section's `TextColumns`, so the selection is the working tool here — it was simply never put down. The transcriber's next keystroke would have replaced the whole of the new columns. It now collapses to the start before the screen comes back | 3.0.345 | — |
| 9/3/2026, review | 3.0.339 | 91 "Object variable or With block variable not set" | `Lp_Table_Mark_Keep_With_Next` | a row-and-column Convert to List with *empty paragraph after list groups* ticked but *keep list groups on the same page* NOT ticked | found while converting Table Tools off the scratch document, never reported. The macro is in two halves. The first sets `tbl` inside `If Selection.Information(wdWithInTable) And "S" is set`; the second, the blank line between groups, reads `tbl.Columns.count` with no such guard. With "E" chosen and "S" not, `tbl` is `Nothing`. It never reached a transcriber as anything they could report because `Lp_Table_Convert_RC_Table_To_List` called it through `Application.Run`, which takes the error itself and shows Word's own Run-time error dialog. The table is now passed in, so it cannot be unset | 3.0.345 | — |
| 9/3/2026, review | 3.0.339 | none — a colon recolored, or paragraph marks collapsed, anywhere in the book | `Lp_Table_Convert_RC_Table_To_List`, `Lp_Table_Cleanup_For_Roation_And_List`, `Lp_Table_Mark_Keep_With_Next` | would only have appeared once Table Tools stopped using a scratch document | recorded because it is the trap this whole list of conversions keeps setting. Three passes were written against a scratch document holding one table, where the wrap setting cannot do any harm: the `": "` recolor and the manual-line-break replace both used `wdFindContinue`, and the doubled-paragraph-mark tidy-up started at `Selection.HomeKey wdStory`. On a range in the transcriber's own book each of those means the rest of the chapter. All three are `wdFindStop` on the table's own range now. **Every `Find` in a converted macro must be `wdFindStop`** | 3.0.345 | — |

| 9/3/2026, review | 3.0.345 | none — the Note in the wrong color, and not held with its list | `Lp_Table_Insert_Transcriber_Note` | any Convert to List or Rotate Table | caught in review of the 3.0.345 conversion, before it shipped, and recorded because both were invisible while the note was born in a scratch document. The note's paragraph is now split off the first line of the list, so it inherits from it. **KeepWithNext was written `wdToggle`**, which flips what is there — on a list run with *keep list groups on the same page*, that line already carries KeepWithNext as direct formatting and `Box Blue` has none of its own to overrule it, so the toggle turned it OFF and the note could be left at the foot of a page with its list overleaf. It is `= True` now. **And the font color** came from wherever the note was born: on a column-header-only list that is the first header, which has just been set to the color the transcriber chose, so the note came out red or blue inside its blue box. Color, italic and underline are now set outright, which is what the scratch document's `Normal` gave for free | 3.0.346 | — |
| 9/3/2026, review | 3.0.345 | none — a rotated table losing its yellow and white striping | `Lp_Table_Transpose_Table` | Rotate Table | the transpose copies the source table's **style** to the new table, and always did. It is only half of how a table looks: which parts of a style Word actually draws — the banded rows above all — are switches on the TABLE (`ApplyStyleRowBands` and its five neighbors), and a table Word has just built carries its own defaults for them. The six are now carried across, so the striping comes back as it was and stays off where the transcriber had turned it off. Not a regression — this was true of the old code too, and only came to light because Jerry asked about the striping | 3.0.346 | — |

| 9/3/2026, Jerry | 3.0.345 and 3.0.347 (never released) | none — **Word closes outright**; nothing raised, nothing logged | `Lp_Table_Convert_Options_Form` — Convert Table to List AND Rotate Table | both, every time | Jerry: *"converting table to a list and transposing (rotating) a table, both crashed word"*. **A CUSTOM UNDO RECORD, ADDED BY THE CONVERSION — NOT A FAULT IN THE FEATURE, AND THE SAME DEFECT AS THE `Lp_TOC_CleanAndFormat_TOC` ROW ABOVE.** Taking Table Tools off the scratch document also wrapped each conversion in `StartCustomRecord` so Ctrl+Z would take it back in one press. A `Find` with `Replace:=wdReplaceAll` inside an open custom undo record kills Word — access violation in `wwlib.dll` — and every path through here runs at least four of them via `Lp_Table_Replace_In_Table`, the row-and-column list a fifth for the colon recolor. **Already measured with three canaries on 9/2/2026; see that row rather than measuring it a third time.** **Cured at 3.0.348 by putting the ROUND TRIP back, hidden.** Removing the record alone left Ctrl+Z at 30 to 50 presses, which Jerry rejected outright — *"I think that pressing ctrl-z 30 to 50 times (depending on the size of the table) is not tollerable for anyone"* — and his own suggestion is the fix: *"why not save the original table and restore it with one undo?"* The scratch document does two jobs and only the first was ever the fault: it had to be SHOWN because the passes reached their table through `Selection`, and it is ALSO what keeps the undo short, because edits made in another document are not in this book's undo stack at all. `Lp_Table_Convert_Hidden` makes it hidden, runs every pass on a range, and brings the result home by `FormattedText` — two Ctrl+Z presses, the same as the old list conversion and better than the old rotation's four (see the 3.0.349 row below for why it cannot be one). **The lesson is about this register, not about Word:** the 9/2/2026 row was already here and was missed because the check was a search for the feature being worked on — striping, rotation — and not for the mechanism being introduced. Read the rows about the technique as well as the rows about the button | 3.0.348 | — |

| 9/3/2026, Jerry | 3.0.349 (never released) | none — the finished list appears **inside the table it was made from** | `Lp_Table_Convert_Hidden` | Convert Table to List, every table type | Jerry: *"Convert Table to List is placing the list data in a table"*. The result was brought home with `work.FormattedText = body.FormattedText`, where `work` was the original table's own range. **Assigning `FormattedText` to a range that IS a table does not replace the table** — Word keeps the table and puts the incoming content into its cells. The table is deleted first now and the result lands where it stood, which is what the old clipboard route did. **It cannot be one undo step:** making it one would mean widening the range to take in a paragraph mark outside the table so the range is not purely a table, and there is not always one to take — a table at the very end of a book is followed only by the document's permanent final paragraph mark, which cannot be replaced (error 5904), and a table at the very start has nothing before it. Two presses, and dialogs 178 and 200 say two | 3.0.350 | — |

| 9/3/2026, Jerry | 3.0.351 | none — a rotated table comes back with thin borders | `Lp_Table_Transpose_Table` | Rotate Table, on any book whose base font is 22 point or larger | the transpose builds a new table and copies the source's **style** to it, so the style's own borders come across. The heavier weight does not: `Lp_Set_Table_Border_Weights` and the four Table Tools color buttons put it on as **direct formatting**, chosen for the book's base font size by `Lp_Border_Weight_For_Base_Font` — 2 1/4 point up to 6 — and that lived on the old table and died with it. Not caused by the 3.0.350 conversion; it was true of the 9/2/2025 code too, and only came up because the striping question sent someone looking at what else the transpose drops. The six edges, the two diagonals and the shadow are now copied from the source. **Copied, not recomputed:** `Lp_Border_Weight_For_Base_Font` reads the `Lp_Base_Font_Size` Public, which holds whatever was chosen the last time a template was attached in this Word session — the exact staleness the color buttons were fixed for on 8/8/2026 — and copying also carries a weight the transcriber set by hand | 3.0.352 | — |

| 9/3/2026, Jerry | 3.0.353 | none — the title bar says **"Microsoft Word"**, or **"VistaType"** without the LP | `Sh_Copy_Ref_Pg_Tags_To_Temp_File` and twelve messages across large print | Jerry: *"i'd like you to look at all of the user-facing messages for large print to make sure that the title bar says VistaType LP... i've seen some without LP"* | three faults, found by surveying every message title in the project. **(1)** `Sh_Copy_Ref_Pg_Tags_To_Temp_File` set the label to `"VistaType"`, which is the title bar of **five** `$pg` validation messages in `ShNonModalMessage` — and the declaration of `Sh_PgVal_TitleText` there has read `' "VistaType LP" or "Braille Macros" '` all along, so the intent was never in doubt. **(2)** **Twelve** large-print messages passed no title argument at all, and a `MsgBox` with no title is captioned **"Microsoft Word"** — the transcriber cannot tell it is VistaType LP speaking, and has no number to quote. They now carry 289 to 301. **(3)** `Sh_Ask`'s `MsgBox` fallback — the one that runs when `Sh_Message_Form` refuses to appear — dropped the title, so the dialog shown *because* something had already gone wrong was the one that named nothing; and dialogs 203, 205 and 206 carried a double space. **A second pass at 3.0.356 finished the job**, on Jerry's word. Widening the search from "no title" to "title that is not `VistaType LP (nnn)`" found more: three LARGE PRINT messages said something else entirely — `Lp_Copy_To_Temp_Doc`'s "Template Error" (the missing `LargePrintTemplate.dotx`), `Lp_Attach_The_Template`'s "Save As Canceled", both now 305 and 306. The three braille messages took `Braille Macros (302)`-`(304)`; eight more braille ones said `Braille Macros` with no number at all and now carry one — the three in `Sh_Delete_Prodnote_Paragraphs` reuse the large print half's own number (228, 229, 230), because it is one dialog reached from two tabs. **Two titles carry no product label on purpose, Jerry's call:** `Document Settings` (`Sh_Doc_Info`) and the six DAISY/NIMAS converter messages are reached from BOTH ribbon tabs, so naming one of them would be wrong. Doc Info keeps its three bare titles; the DAISY/NIMAS six keep their own words and took numbers 308-313 | 3.0.356 | — |

| 9/3/2026, Jerry | 3.0.361-3.0.363 (never released) | none — a new feature that simply never runs | `VtBrailleTabShown` (removed) | clicking the **Braille Macros** tab on an unsaved SWIFT book | Jerry: *"clicking the Braille Macros tab did nothing."* **A TECHNIQUE THAT CANNOT WORK IN THIS PRODUCT — do not try it again.** He asked for a message when the Braille Macros tab is activated: *"When the user clicks that tab, it is obvious that they intend to some of braille macros to they need to get that file saved!"* Office has **no "tab was clicked" event** — no `onAction` on a `<tab>`, nothing that reports activation — so the technique was to hang a `getEnabled` on the first button of the tab and rely on Word asking for it when it first draws that tab. The first suspect when it stayed silent was Word caching a control's answer; that was wrong and a re-arm on the document events did not help. The real reason is structural: **from 3.0.118 the installer ALWAYS writes both tabs into the transcriber's own `Word.officeUI`**, and the embedded tabs in `customUI14.xml` are hidden by `VtTabVisible` whenever it has. The tab anyone clicks is therefore never this add-in's own, and Word never asks this add-in's callbacks about it. Removed at 3.0.364, with tombstones in `customUI14.xml` and `RibbonCallbacks.bas`. **Anything that must happen when a transcriber reaches for the braille tools belongs in `Dx_Is_BANA_Template_Attached`**, which all fifteen braille buttons run first — that is where `Dx_Prompt_Save_Before_Braille` is called from, and it cannot be cached or bypassed | 3.0.364 | — |

*(Nothing in this register has shipped yet — the whole of it is on `dev`. Fill "Shipped in" when
the release goes out.)*
